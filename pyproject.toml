[tool.poetry]
name = "sweepy"
version = "0.1.0"
description = "Sweepstakes generator"
authors = ["Rory Devitt <rory.devitt@wegift.io>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
